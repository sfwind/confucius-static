const P = "_$_$_"

export const SET = `${P}/set`
export const PUSH = `${P}/push`
export const SPLICE = `${P}/splice`
export const START = `${P}/start`
export const END = `${P}/end`
export const OPEN = `${P}/open`
export const CLOSE = `${P}/close`
export const START_LOAD = `${P}/start_load`
export const END_LOAD = `${P}/end_load`

export const ActionMap = {
	set: SET,
	push: PUSH,
	splice: SPLICE,
	start: START,
	end: END,
	open: OPEN,
	close: CLOSE,
	startLoad: START_LOAD,
	endLoad: END_LOAD
}
